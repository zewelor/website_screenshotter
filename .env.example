# Compose command: podman-compose, "docker compose" (V2), or docker-compose (V1 legacy)
# Leave empty for auto-detection (podman-compose -> docker compose -> docker-compose)
#COMPOSE_CMD=podman-compose
